{{- $packages := (includeTemplate "packages.tmpl" .) | fromYaml -}}
{{- $hasPiAgent := false -}}
{{- range $packages.packages -}}
  {{- if eq .name "pi-coding-agent" -}}
    {{- $hasPiAgent = true -}}
  {{- end -}}
{{- end -}}
{{- if not $hasPiAgent }}
*
{{- end }}
